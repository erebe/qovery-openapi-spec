type: object
properties:
  cloud_provider:
    $ref: ./CloudProvider.yaml
  credentials:
    type: object
    properties:
      id:
        type: string
        format: uuid
      name:
        type: string
  region:
    type: string
